# LuminaLang 编程语言官方文档

# 一、语言概述

LuminaLang（简称 Lum）是一门**强类型、解释型、图灵完备**的原创编程语言，核心设计简洁易用，同时具备完整的流程控制与函数能力，可用于编写各类控制台算法程序、逻辑计算任务，适配入门学习与简单项目开发。

核心特性：

- 图灵完备：支持递归、while循环，可实现任意可计算算法

- 强类型：仅内置i32（32位有符号整数）类型，类型错误会直接提示

- 简洁语法：无冗余符号，语句无需分号，代码可读性强

- 核心功能：支持变量定义与赋值、分支判断、循环、函数定义与调用、输入输出

# 二、快速入门（Hello, World 示例）

## 2\.1 最简示例（循环输出1）

以下代码可实现“无限循环输出1”，按 Ctrl\+C 安静退出，是验证语言运行环境的最简示例：

```lum
fn main() i32 {
    let a:i32 = 1
    while a == 1 {
        out 1
    }
    back 0
}
```

## 2\.2 运行方法

1. 将上述代码保存为 `test.lum`（文件后缀必须为 \.lum）

2. 确保已保存完整的解释器文件 `interpreter.py`

3. 在控制台执行命令：`python interpreter.py test.lum`

4. 运行效果：控制台持续输出1，按 Ctrl\+C 无报错安静退出

# 三、基础语法规则

- 程序入口：必须定义 `main` 函数，程序从 main 函数开始执行，无 main 函数会报“未找到main”错误

- 代码块：所有逻辑（函数体、循环体、分支体）必须用 `{ }` 包裹，不可省略

- 语句规则：语句无需分号结尾，一行可写一条语句，多语句换行书写即可

- 注释：**不支持任何注释**（单行//、多行/\* \*/均不支持），写注释会直接报语法错误

- 标识符规则：变量名、函数名仅支持字母、数字、下划线，不能以数字开头（如 a1、\_test 合法，1a 非法）

- 大小写敏感：fn、let、while、out、back 等关键字区分大小写，必须小写书写

# 四、数据类型与字面量

## 4\.1 内置数据类型

目前 LuminaLang 仅内置一种基础数据类型：

|类型|说明|示例|
|---|---|---|
|i32|32位有符号整数，支持正数、负数、0|0、5、\-10、100|

## 4\.2 字面量

仅支持整数字面量，直接书写数字即可，无需加引号或其他修饰：

```lum
// 合法字面量（无注释，仅示例说明）
let num1:i32 = 0
let num2:i32 = 100
let num3:i32 = -20
```

注意：不支持浮点数、字符串、布尔值等其他类型，书写非整数字面量会报语法错误。

# 五、变量操作

## 5\.1 定义变量

语法格式（必须指定类型，且类型只能是i32）：

```lum
let 变量名:i32 = 表达式
```

示例：

```lum
// 定义变量并赋值
let a:i32 = 5
let b:i32 = a + 3  // 支持表达式赋值
let c:i32 = input() // 用输入函数赋值
```

注意：变量必须先定义再使用，未定义直接使用会报语法错误；定义时必须赋值，不能只定义不赋值。

## 5\.2 变量重新赋值

语法格式（无需重复写let和类型，直接赋值即可）：

```lum
变量名 = 表达式
```

示例：

```lum
let a:i32 = 5
a = 10  // 重新赋值为10
a = a + 5  // 赋值为表达式结果（15）
```

# 六、运算符

LuminaLang 支持算术运算符、比较运算符，所有运算符仅适用于i32类型。

## 6\.1 算术运算符

|运算符|说明|示例|结果|
|---|---|---|---|
|\+|加法|3 \+ 5|8|
|\-|减法|10 \- 4|6|
|\*|乘法|2 \* 6|12|
|/|整数除法（向下取整）|7 / 2|3|

## 6\.2 比较运算符

比较结果为整数：1 表示“真”，0 表示“假”，常用于分支、循环的条件判断。

|运算符|说明|示例|结果（真/假）|
|---|---|---|---|
|==|等于|5 == 5|1（真）|
|\!=|不等于|5 \!= 3|1（真）|
|\&lt;|小于|2 \&lt; 5|1（真）|
|\&gt;|大于|6 \&gt; 10|0（假）|
|\&lt;=|小于等于|4 \&lt;= 4|1（真）|
|\&gt;=|大于等于|7 \&gt;= 9|0（假）|

# 七、输入与输出

## 7\.1 输入（input\(\)）

内置输入函数，无需参数，仅接收整数输入，输入非整数会报类型错误。

语法：`input()`

示例：

```lum
// 接收用户输入的整数，赋值给变量a
let a:i32 = input()
out a  // 输出输入的整数
```

## 7\.2 输出（out）

内置输出语句，用于输出i32类型的值（变量、字面量、表达式均可），每次输出一个值，自动换行。

语法：`out 表达式`

示例：

```lum
out 123  // 输出字面量
let b:i32 = 8
out b + 2  // 输出表达式结果（10）
out input()  // 输出用户输入的整数
```

# 八、流程控制

## 8\.1 分支判断（if / elif / else）

语法格式（elif可多个，else可选）：

```lum
if 条件表达式 {
    逻辑语句
} elif 条件表达式 {
    逻辑语句
} else {
    逻辑语句
}
```

说明：条件表达式的结果必须是1（真）或0（假），仅当条件为1时，执行对应代码块。

示例：

```lum
fn main() i32 {
    let num:i32 = input()
    if num > 10 {
        out 100
    } elif num == 10 {
        out 10
    } else {
        out 1
    }
    back 0
}
```

## 8\.2 循环（while）

语法格式（当条件为1时，循环执行代码块，条件为0时退出循环）：

```lum
while 条件表达式 {
    逻辑语句（可包含变量赋值、out、函数调用等）
}
```

示例1：循环累加（从1加到10，输出总和）

```lum
fn main() i32 {
    let sum:i32 = 0
    let i:i32 = 1
    while i <= 10 {
        sum = sum + i
        i = i + 1
    }
    out sum  // 输出55
    back 0
}
```

示例2：无限循环（按Ctrl\+C退出）

```lum
fn main() i32 {
    while 1 == 1 {
        out 1
    }
    back 0
}
```

# 九、函数定义与调用

## 9\.1 函数定义

语法格式（必须指定返回值类型，参数需指定类型，返回值用back语句）：

```lum
fn 函数名(参数1:类型, 参数2:类型, ...) 返回值类型 {
    逻辑语句
    back 返回值（必须是返回值类型的表达式）
}
```

说明：

- 返回值类型仅支持i32

- 参数可多个，用逗号分隔，每个参数必须指定类型（i32）

- 函数体必须包含back语句，用于返回结果；无back语句会默认返回0

- 函数需定义在main函数之外或main函数之前

示例：定义一个加法函数

```lum
// 定义加法函数，接收两个i32参数，返回它们的和
fn add(a:i32, b:i32) i32 {
    back a + b
}

fn main() i32 {
    let res:i32 = add(3, 5)
    out res  // 输出8
    back 0
}
```

## 9\.2 函数调用

语法格式：`函数名(参数1, 参数2, ...)`

说明：参数数量、类型必须与函数定义一致，否则会报类型错误。

示例：调用函数实现递归求阶乘

```lum
// 递归求阶乘函数
fn factorial(n:i32) i32 {
    if n == 1 {
        back 1
    }
    back n * factorial(n - 1)
}

fn main() i32 {
    let num:i32 = input()
    let res:i32 = factorial(num)
    out res
    back 0
}
```

# 十、常见错误与解决方案

|错误提示|原因|解决方案|
|---|---|---|
|语法错误|1\. 写了注释；2\. 代码块未用\{ \}包裹；3\. 变量/函数名不合法；4\. 运算符使用错误|1\. 删除所有注释；2\. 给逻辑代码添加\{ \}；3\. 修正变量/函数名；4\. 检查运算符格式|
|类型错误|1\. 输入非整数；2\. 函数参数/返回值类型不匹配；3\. 变量赋值非i32类型|1\. 输入整数；2\. 修正参数/返回值类型；3\. 确保赋值为整数或整数表达式|
|未找到main|未定义main函数，或main函数名写错（如Main、MAIN）|定义正确的main函数：fn main\(\) i32 \{ \.\.\. \}|
|正常中断提示|无需处理，最新版解释器已隐藏该报错，按Ctrl\+C会安静退出||
